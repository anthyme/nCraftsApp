<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:b="clr-namespace:Xamarin.Behaviors;assembly=Xamarin.Behaviors"
             x:Class="NCrafts.App.Sessions.SessionsView">
  <StackLayout x:Name="root">
    <Label Text="{Binding Header}"></Label>
    <ListView ItemsSource="{Binding Sessions}" VerticalOptions="Fill" HorizontalOptions="Fill">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Padding="15,0">
              <Label Text="{Binding Title}" />
            </StackLayout>
            <b:Interaction.Behaviors>
              <b:BehaviorCollection>
                <b:EventToCommand
                    EventName="Tapped"
                    Command="{Binding Path=BindingContext.SelectSessionCommand, Source={x:Reference root}}"
                    CommandParameter="{Binding Id}" />
              </b:BehaviorCollection>
            </b:Interaction.Behaviors>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>